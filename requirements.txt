python-dotenv>=1.0.0
# ccef-connections installs from a local clone whose path differs per machine —
# install it editable, separately ([sheets] brings gspread + google-auth, >= 0.2.0):
#   pip install -e "<path-to-ccef-connections>[sheets]"
# `sync_projects.py --setup dynamic-action-map` (meta-project) does this automatically.
